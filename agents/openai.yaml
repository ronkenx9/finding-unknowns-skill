interface:
  display_name: "Finding Unknowns"
  short_description: "Surface blind spots before implementation"
  default_prompt: "Use $finding-unknowns to audit assumptions and blind spots before we implement this."
